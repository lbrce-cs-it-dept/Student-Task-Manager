[project]
name = "simple"
version = "0.1.0"
description = "Student Task Manager - Django Web Application"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
    "gunicorn>=26.2.0",
    "whitenoise>=6.12.0",
]

[tool.uv]
package = false
